//TODO: this UI doesn't support multiple users yet, but the backend db does.
//  For now, use a pre-existing user ID throughout the app.
export const USER_ID = 1;

export const PRIORITY = Object.freeze({
  low: 3,
  medium: 2,
  high: 1,
});
